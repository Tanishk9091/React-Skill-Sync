# MongoDB connection string. Replace <username>, <password>, <cluster> and <dbname> as appropriate.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Port for the server
PORT=4000
